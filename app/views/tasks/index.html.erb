<h1> My tasks </h1>

<% @tasks.each do |task| %>
  <% if task.completed %>
  <h3>[X] <%= link_to task.title, show_task_path(task.id) %></h3>
  <% else %>
  <h3>[ ] <%= link_to task.title, show_task_path(task.id) %></h3>
<% end %>
<% end %>


<%= link_to("+add task", new_path) %>
